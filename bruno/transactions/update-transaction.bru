meta {
  name: Update Transaction
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/transactions/{{transactionId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{authToken}}
}

body:json {
  {
    "amount": 60,
    "description": "Taxi"
  }
}
