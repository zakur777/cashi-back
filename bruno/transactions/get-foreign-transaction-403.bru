meta {
  name: Get Foreign Transaction 403
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/transactions/{{foreignTransactionId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{userBToken}}
}

script:post-response {
  test("returns 403 for another user's transaction", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
